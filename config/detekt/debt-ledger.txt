# Technical-debt ratchet ledger.
#
# These are CEILINGS, not targets: they may only ever be LOWERED.
# scripts/check_debt_ratchet.sh fails the build if the real counts exceed them.
# When a phase reduces a number, lower it here in the SAME commit — that is what locks the gain in.
#
# See docs/reports/WORLD_CLASS_ROADMAP.md (§1 exit criteria, §5 ledger).
#
# key=max_allowed
TOTAL=8
NestedBlockDepth=0
LongParameterList=1
LargeClass=1
ComplexCondition=0
SwallowedException=0
PrintStackTrace=0
ExplicitGarbageCollectionCall=0
ImplicitDefaultLocale=0

# File-size ceilings (2026-09-02). detekt's LargeClass counts CLASSES at 600 and only what is
# baselined, so a file can grow past the limit in CLAUDE.md without a single new baseline ID -
# BasePlayerFragment went 1491 -> 1771 that way. These count Kotlin FILES under app/src/main/java
# by line (awk NR / readLines), measured by both front-ends. Same rule: only ever LOWERED.
KotlinFilesOver600Lines=15
KotlinFilesOver500Lines=34
