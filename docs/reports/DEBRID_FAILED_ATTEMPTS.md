# Debrid Failed Attempts

## Purpose
Canonical failed-attempts file for Debrid module tasks.

## Entries
- `TASK 000-DEBRID-DEEP-AUDIT-STREMIO-COMPARISON`: Audit-only task. No failed implementation attempt occurred. Risks documented: sensitive Debrid/token URL logging, cache-state inference instead of verification, Real-Debrid-only implementation despite multi-provider language, and incomplete Stremio add-on parity.
- `TASK 001-DEBRID-PHASE1-SECURITY-LOG-REDACTION`: Initial Gradle validation was slow because `:app:compileDebugKotlin --offline` timed out twice and `--dry-run` also timed out before useful output. After stopping stale daemons and allowing the full build to complete, `:app:assembleDebug --offline` passed. No failed implementation remains open.
- `TASK 001B-DEBRID-SWARM-REVIEW-FOLLOW-UP`: Swarm review identified incomplete first-pass log coverage and missing persisted QA evidence. Follow-up fixes were implemented and rebuilt successfully; no failed implementation remains open. Full manual Real-Debrid playback QA remains a separate unclaimed scope.
- `TASK 002-DEBRID-PHASE2-RD-CACHE-VERIFICATION`: No failed implementation remains open. Functional PASS is not claimed because source badge/filter behavior and Real-Debrid playback/resume still require manual QA with an authenticated Real-Debrid account.
- `TASK 003-DEBRID-PHASE3-PLAYBACK-ERROR-AUDIT`: Manual QA found provider-side copyright/legal failures, `UNKNOWN` source unavailability, and HTTP `429` add-magnet rate limiting. These are not solved by Phase 2 cache labels. Follow-up implementation is needed for typed failure classification, cooldown/no-retry behavior, and clearer user messages.
- `TASK 004-DEBRID-PHASE3A-LITE-RD-ERROR-COOLDOWN`: `clean assembleDebug` timed out at the command layer after 15 minutes and its final exit code was not captured, although the wrapper process later finished. Follow-up `:app:assembleDebug` passed, install/launch smoke passed on both requested devices, and no failed implementation remains open. Full Real-Debrid playback behavior is still not functionally PASS until blocked-source and good-source manual QA are performed with an authenticated Real-Debrid account.
- `TASK 004B-DEBRID-PHASE3A-RATE-LIMIT-FOLLOW-UP`: Manual QA found a Phase 3A behavior gap: after one blocked/rate-limited attempt, subsequent clicks could appear to do nothing because global cooldown waited silently. Follow-up now returns immediate cooldown errors and further reduces availability-check pressure. Manual retest is still needed to confirm the user-facing rate-limit flow under the current Real-Debrid account/IP state.
- `TASK 005-DEBRID-PHASE3D-HINDI-GERMAN-SOURCE-RECOVERY`: Audit found stale Torrentio config and inactive built-in dynamic registries: no `language` priority, old `quality=` key, small `limit=20`, limited provider set, and no automatic PureFire registry load. Follow-up is implemented and build-smoke passed, but functional PASS is not claimed until the same failing Hindi/German titles are manually retested with Real-Debrid because provider-side copyright/legal removals can still block individual sources.
- `TASK 006-DEBRID-CUSTOM-SCRAPER-BLOCKED-SOURCE-FOLLOW-UP`: Manual QA found that custom JSON scraper sources can stop on `This source is blocked by Real-Debrid`. This cannot be bypassed when Real-Debrid blocks that hash/link. Follow-up now auto-skips safe terminal source failures to the next candidate, but full functional PASS still requires the user's same registry/title retest.
- `TASK 008-STREMIO-ADDON-URL-PHASE1`: A delete-first migration was avoided because raw Stremio manifests are not compatible with the old registry-definition fetcher. Kotlin daemon validation timed out until the compiler was forced in-process. `clean :app:assembleDebug` also timed out after removing APK outputs, so final validation used non-clean `:app:assembleDebug`, install, and launch smoke. Full functional PASS remains blocked on manual QA with a real configured Stremio addon and Real-Debrid account state.
- `TASK 009-STREMIO-DIRECT-PLAYBACK-RD-CONFIG-FIX`: Manual QA found links appeared but playback showed Real-Debrid configuration missing. Root cause was direct Stremio/AIOStreams URLs being routed through app-side Debrid metadata/auth paths. This is fixed for direct URLs, but if the addon server itself returns a Real-Debrid config missing response, the user's configured manifest URL must be corrected in the addon.
- `TASK 010-STREMIO-DIRECT-DEBRID-LABEL-FIX`: Manual QA found direct Debrid playback worked but player controls showed IPTV, with possible `API exception please try again` behavior. Root cause was using null/IPTV source identity to avoid app-side Debrid resolver calls. Follow-up keeps Debrid identity and uses a separate direct-play guard so resolver/API paths remain disabled for direct addon URLs. Manual retest is still needed on the same movie/series source to confirm the label and API exception are gone during real playback.
- `TASK 012-STREMIO-DIRECT-DEBRID-EPISODE-CONTINUITY-RESUME`: Manual QA previously found direct Debrid `DPAD_DOWN` reported episodes unavailable and resume could rely on expiring direct addon URLs. The implementation now reuses the existing episode browser and fresh-resolves from stable metadata, but functional PASS is not claimed until the same direct Stremio/AIOStreams series is manually tested. `192.168.0.21:5555` was unreachable during install QA (`adb connect` timed out with `10060`).
