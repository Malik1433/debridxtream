# Debrid Module Report
Status: canonical/partial
Scope: Debrid source selection, resolution, playback, and provider setup.

## Current Active Runtime Flow
- Debrid browse/search opens `MovieDetailActivity` or `SeriesDetailActivity` with Debrid flags/metadata.
- Source picker uses unified source discovery and preserves provider/source profile metadata.
- Resolver-backed Debrid playback resolves magnet/infoHash through `PlaybackResolver` before `PlayerActivity`.
- Direct HTTP/addon playback keeps `PlaybackSource.DEBRID` identity but uses direct-play guards to skip app-side Real-Debrid resolver calls.
- Resume should fresh-resolve durable metadata instead of trusting expired direct URLs.
- Debrid Continue Watching IDs now mirror canonical watched-state identity keys; raw info-hash fallbacks are not used as durable CW IDs.

## Important Active Files
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/debrid/DebridFragment.kt` - Debrid home/browse/resume entry.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/vod/MovieDetailActivity.kt` - Debrid movie details, source picker, resolver handoff.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/series/SeriesDetailActivity.kt` - Debrid series episode source picker and player handoff.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/player/stabilized/DebridResolutionManager.kt` - player-side Debrid refresh/next episode resolution.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/data/debrid/repository/PlaybackResolver.kt` - Debrid playback resolver contract.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/series/SourceSelectionBottomSheet.kt` - shared Debrid source picker UI.

## What Must Not Break
- Source picker filtering and selected source continuity.
- Resolver path for magnet/infoHash Real-Debrid sources.
- Direct HTTP/addon path and headers.
- Real-Debrid auth/config path.
- Debrid identity in Player controls/history; do not downgrade to IPTV.
- Sensitive token, provider URL, magnet, infoHash, and direct stream redaction.

## Known Bugs / Open Issues
- Full Stremio parity is not implemented.
- Provider-cache verification still needs manual QA where relevant.
# Debrid Module Report
Status: canonical/partial
Scope: Debrid source selection, resolution, playback, and provider setup.

## Current Active Runtime Flow
- Debrid browse/search opens `MovieDetailActivity` or `SeriesDetailActivity` with Debrid flags/metadata.
- Source picker uses unified source discovery and preserves provider/source profile metadata.
- Resolver-backed Debrid playback resolves magnet/infoHash through `PlaybackResolver` before `PlayerActivity`.
- Direct HTTP/addon playback keeps `PlaybackSource.DEBRID` identity but uses direct-play guards to skip app-side Real-Debrid resolver calls.
- Resume should fresh-resolve durable metadata instead of trusting expired direct URLs.
- Debrid Continue Watching IDs now mirror canonical watched-state identity keys; raw info-hash fallbacks are not used as durable CW IDs.
- Debrid series/episode watched identity tracking is stabilized through canonical `WatchedIdentityBuilder` keys, builder-level unsafe-ID validation, explicit 40-char hex / 32-char base32 info-hash rejection, and collision-resistant fallback keys when upstream metadata is incomplete.
- Debrid Continue Watching identities are aligned with the same hardened `WatchedIdentityBuilder` contract: stable TMDB/IMDb IDs are preferred, title/year fallback keys are used when stable IDs are absent, and raw info-hashes are not stored as CW durable identifiers.

## Important Active Files
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/debrid/DebridFragment.kt` - Debrid home/browse/resume entry.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/vod/MovieDetailActivity.kt` - Debrid movie details, source picker, resolver handoff.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/series/SeriesDetailActivity.kt` - Debrid series episode source picker and player handoff.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/player/stabilized/DebridResolutionManager.kt` - player-side Debrid refresh/next episode resolution.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/data/debrid/repository/PlaybackResolver.kt` - Debrid playback resolver contract.
- `app/src/main/java/com/tvonnet/debridxtreamiptv/ui/series/SourceSelectionBottomSheet.kt` - shared Debrid source picker UI.

## What Must Not Break
- Series episode source discovery returning full lists across multiple providers.
- Real-Debrid token validation and auto-refresh pre-flight sequence.
- Direct addon/proxy readiness checks and the skip-resolver logic for `DIRECT_STREAM` cached status.
- UI source filtering using the unified `SourceFilterState` mapping backwards-compatible with `MovieDetailActivity` while supporting new TV-friendly chip filters (Quality, Language, Type) in `SourceSelectionBottomSheet`.
- The `isPlaybackReady` rule: Direct HTTP links are `DIRECT_STREAM`, not `VERIFIED_CACHED`. The UI "Cached" chip strictly filters for `VERIFIED_CACHED` (e.g. Torrentio).

## Known Bugs / Open Issues
- Full Stremio parity is not implemented.
- Provider-cache verification still needs manual QA where relevant.

## Latest Change
- **Debrid Discover Dropdown Font Crash Fix (2026-06-08):** Fixed the runtime crash that occurred when opening any Debrid Discover dropdown. Root cause was `DropdownAdapter.getView()` doing a direct `ResourcesCompat.getFont(... R.font.plus_jakarta_sans)` lookup for each row, which failed on the target Fire TV with `Resources$NotFoundException` and killed the process. Dropdown rows now reuse the already-resolved `btnType.typeface`. Type/Catalogue/Genre/Year filtering, backend parameters, poster grid, sidebar, playback, source picker, watched state, and Continue Watching remain unchanged. `:app:compileDebugKotlin` and `:app:assembleDebug` passed; APK installed on `192.168.178.35:5555`; device QA confirmed all dropdown classes open without the font crash, BACK closes dropdowns/returns to Debrid, and logcat fatal scan stayed clean. Pagination/detail QA was not expanded beyond this minimal regression scope.

- **Debrid Sidebar Search/Home/Discover Restore (2026-06-08):** Restored the compact Debrid rail to exactly three icon-only actions: Search, Home, and Discover. The previous `nav_item_discover` Home-behavior regression is split into a dedicated Home control, while Discover now opens the existing `DebridDiscoverActivity` through a new local compass-style vector. Search behavior, Home top-content behavior, sidebar focus protection, hero, rows, source picker, resolver, playback, Continue Watching, and watched features remain unchanged. `:app:compileDebugKotlin` and `:app:assembleDebug` passed, the APK was installed on `192.168.178.35:5555`, and user manual QA confirmed the rail is working.
- **Direct Proxy Readiness / Extraction Loop Guard (2026-06-03):** MediaFusion/AIO/Stremio direct-proxy readiness now keeps typed `READY`, `UNCERTAIN`, and `TERMINAL` decisions through Movie and Series launch paths. Terminal addon/API/not-cached redirects are blocked before Player launch and marked failed for the current picker session; uncertain links remain explicit-click only and are not promoted to RD cached. Player no-first-frame direct-proxy timeouts now return to the source picker/next candidate instead of entering a same-URL or full extraction loop. Provider pseudo-error stream filtering was expanded for API exception, quota/limit, not cached/not ready, timeout, and provider-error rows.
- Direct addon and direct Debrid edge cases stay in task-specific docs.
- **Open Issue A (2026-05-25) FIXED (2026-05-28):** MediaFusion and AIO addon sources throwing API exceptions during Debrid source resolution. Fixed by updating addon request User-Agent to `Stremio/1.0`, increasing timeouts to 20s, and adding `Accept: application/json` headers in `StremioAddonFetcher` and `AddonCatalogServiceFactory`. Manual QA confirmed: addon links working and API error no longer appears.
- **Open Issue B (2026-05-25):** Debrid Continue Watching resume intermittently fails (sometimes works, sometimes does not). Root cause not yet diagnosed.
- **Open Issue C (2026-05-25):** Debrid expired/null `expiresAt` Continue Watching resume starts playback but then stalls on reloading after seconds/minutes. Likely in network/stall recovery path (Phase 2 area, untouched). Next recommended task: Diagnose Only (no fix without approval).

## Recent Fixes
- **Debrid Playback Stop-After-Some-Time Recovery Hardening (2026-06-03):** Hardened the shared player recovery path for Debrid long playback. Direct Debrid/addon recoverable player errors and buffer timeouts now attempt a fresh source-profile refresh with direct passthrough preserved for fresh provider results, while resolver-backed Debrid retry keeps passthrough disabled. `READY`-state stalls now emit `stall_warning` before escalation and require multiple Debrid stall windows. `:app:compileDebugKotlin` and focused Debrid passthrough/repository tests passed; device long-run QA on `192.168.0.21:5555` remains the closure gate.
- **TorBox Runtime Integration**: Added an isolated runtime path for a later service-neutral `+ TorBox` picker action. `TorBoxPreferences` stores only the TorBox token in separate encrypted preferences, `TorBoxApiService` implements the confirmed multipart magnet enqueue and `mylist` status endpoints, `TorBoxSourceActionRepository` accepts `AddableTorrentIdentity`, builds a canonical magnet from validated infoHash when needed, and maps only the confirmed cached contract to `READY`. `completed` remains non-ready. Existing RD repositories, playback behavior, and picker UI remain untouched. `:app:compileDebugKotlin` and `:app:assembleDebug` passed. Live repository invocation and existing-playback device regression QA remain pending for later token/UI wiring.
- **TorBox Addable Identity Plumbing**: Added nullable typed `AddableTorrentIdentity(infoHash, magnet?)` metadata to `MovieSource`. `UnifiedSourceProvider` populates it only from explicit normalized addon infoHash or a hash extracted from a validated magnet. Existing playback fields, direct HTTP handling, cache semantics, and source-picker behavior remain unchanged; direct addon/proxy HTTP URLs are not treated as addable torrent identity. `:app:compileDebugKotlin` and `:app:assembleDebug` passed after stopping a stalled Gradle daemon and rerunning with `--no-daemon --console plain`. Manual source-picker/playback regression QA remains pending.
- **Debrid Series Source Reliability Fix**: Hardened the Debrid series/AIO episode path so `UnifiedSourceProvider` preserves same-infoHash variants when language, provider/source name, source type, file index, or binge group differ, and `SeriesDetailActivity` now refuses to silently fall back to episode 1 when selected episode metadata is missing. QA on `Spider-Noir` confirmed episode 1 and episode 3 source lists stay episode-specific (`S01E01` vs `S01E03`), source counts differ (`314` vs `293`), `DIRECT` badges remain unchanged, and AIOStreams plus StreamThru rows are still visible. `:app:compileDebugKotlin` and `:app:assembleDebug` passed, and the APK was installed on `192.168.0.21:5555`.
- **Debrid Continue Watching Key Alignment Complete**: Refactored Debrid Continue Watching identity generation in `PlayerHistoryManager.kt` so CW content IDs mirror the canonical watched-state keys. Debrid CW no longer falls back to raw info-hashes when TMDB/IMDb metadata is missing; it uses the hardened title/year and collision-resistant episode fallback contract already enforced by `WatchedIdentityBuilder`. Completion-threshold cleanup now targets the same aligned key family used by `watched_state`.
- **Debrid Watched Identity Hardening Complete**: Stabilized Debrid series/episode watched-state identity generation in `WatchedIdentityBuilder.kt` and `PlayerHistoryManager.kt`. Builder-level validation now rejects transient URLs, magnets, token/credential-like values, authorization/API-key parameters, and raw torrent info-hashes matching 40-character hex or 32-character base32 forms before they can become durable watched IDs. Valid high-integrity TMDB/IMDb/stable IDs keep their existing key prefixes. Debrid title fallback keys now include a release-year discriminator when available, and missing/invalid season or episode metadata adds a safe fallback discriminator so unrelated items do not collapse into the same `unknown` episode key.
- **Source Picker Hindi/Multi Language Ranking Fix**: Improved Hindi source detection and selected-language ordering without changing source fetching, playback routing, direct/proxy readiness, or RD Cached semantics. `LanguageParser` now detects `HI`, `HIN`, Hindi-English, dubbed Hindi, dual-audio Hindi, multi-audio Hindi, and Devanagari Hindi markers. `SourceFilterUtils` now treats selected language as an exact-match filter only when matches exist; otherwise it keeps useful fallback sources visible and ranks language-compatible rows first. `SourceSelectionBottomSheet` clears stale selected stream focus when the language changes so focus moves to the first matching row instead of jumping back to an older source.
- **Debrid Source Logging Hygiene Cleanup**: Hardened source-related logging without changing fetching, filtering, playback, UI, or provider behavior. `SensitiveLogRedactor` now returns placeholder values (`<redacted-url>`, `<redacted-magnet>`, `<redacted-hash>`, `<redacted-credential>`) that do not preserve URL/magnet prefixes. Real-Debrid remote logs now redact account IDs, credentials, hashes, torrent IDs, file-selection values, and exception details. Source aggregation logs now use counts/provider summaries instead of raw titles/IDs or per-source dumps. Poster/backdrop image logs now redact artwork URLs. Build/install passed on `192.168.0.84:5555`; app-process log scan after Debrid movie source picker/direct playback found zero raw URL, magnet, token-param, username/password, or long-hash matches.
- **Source Picker Dynamic RD Cached Type Option**: Updated `SourceSelectionBottomSheet.kt` so the Type dropdown is built from the current pre-type filtered source list. `RD Cached` now appears only when at least one `VERIFIED_CACHED` source exists after active Quality/Language filters, and stale unavailable Type selections reset to `All` before filtering. Direct sources remain visible under `All` and `Direct`; the RD Cached predicate remains strict and playback/direct readiness logic was not changed.
- **Source Picker Cached Filter Direct False-Positive Fix**: Prevented click-time direct HTTP/addon readiness from being stored as `VERIFIED_CACHED` in movie and series source lists. `MovieDetailActivity` and `SeriesDetailActivity` now keep ready direct HTTP sources as `DIRECT_STREAM`, while `SourceFilterUtils` only treats `VERIFIED_CACHED` as eligible for the RD cached filter and only shows the cached chip when such a source exists. Direct links remain visible with the filter off and still use the existing readiness preflight before Player launch.
- **Direct Proxy Readiness Centralization**: Centralized MediaFusion/AIO direct addon/proxy readiness classification in `DebridPlaybackRepository.requiresDirectProxyReadinessCheck(url, provider, sourceName, sourceType)`. `MovieDetailActivity` and `SeriesDetailActivity` now call the shared policy before invoking `isAddonProxyPlaybackReady()`, including existing source headers and provider/source metadata, removing duplicated hardcoded classifier copies while preserving direct Debrid playback identity, provider/source profile extras, and resolver-backed hash/magnet flow.
- **Debrid Card Animation Safety QA**: Verification-only pass on `192.168.0.84:5555` finalized the focused card polish after device stress. Rapid D-pad movement across Continue Watching, Trending Movies, and Search results showed stable single-card focus, one active glow, no stuck glow, no recycled-card visual bleed, no visible jank/lag from the pulse animation, readable rating/title/year overlays, and visible Continue Watching progress bars. Continue Watching OK opened `PlayerActivity` and Back restored Debrid focus; Trending OK opened detail/source picker and Back restored Debrid focus. No runtime code changed.
- **Debrid Compact Hero Header Polish**: Reduced the passive Debrid hero in `view_debrid_hero.xml` from a large cinematic title area into a compact metadata header. Hero height changed from `380dp` to `280dp`, title size changed to `30sp` with two-line ellipsized safety preserved, metadata/synopsis spacing was tightened, and synopsis remains capped at two lines. Floating rail, card overlays/animations, source picker, PlayerActivity, and Continue Watching behavior were not changed.
- **Debrid Card + Rating Badge Polish**: Overhauled Debrid content card layout (`item_debrid_content.xml`) and selection animations (`DebridRowsAdapter.kt`). Relocated the focus glow outside the CardView container to prevent edge clipping and anchored it with `-12dp` margins using a multi-layered rich cyan glow (`@drawable/bg_movie_card_glow`). Styled rating badges with a semi-transparent black pill (`@drawable/bg_rating_badge`), amber text, and proper font/padding. Made titles and metadata permanently readable (`alpha = 1.0f` container alpha, passive card opacity `0.8f`, focused `1.0f`). Added standard TV Series overshoot scaling (`1.15f`), dynamic pulsing alpha animation (`0.5f` to `1.0f`), and title marquee on focus. Added safe cleanup/recycling hooks (`clearAnimations()`) in `onViewRecycled` for all three adapters to prevent background thread memory leaks.
- **Debrid Source Picker Polish**: Redesigned the Debrid Source Picker (`dialog_source_selection.xml`) into a Cinematic Bottom 55% Half-Sheet layout using Constraints, moving filters to a horizontal inline header row. Changed `item_movie_source.xml` to an inline horizontal orientation to prevent truncation of long links. Injected Unicode flags into the text badge instead of using standalone views in `MovieSourceAdapter.kt` to keep the layout flat and preserve existing provider UI matching logic.
- **Debrid Hero Metadata + Long-title Polish**: Updated the passive hero layout and binding so hero titles cap at two ellipsized lines, metadata badges only show when rating/year/type data is available, quality remains hidden because no safe quality field is exposed in the current model path, and synopsis stays hidden when overview is missing. Verified on `192.168.0.84:5555` without changing sidebar, rows, cards, source picker, PlayerActivity, or Continue Watching data behavior.
- **Debrid Row Heading + Spacing Polish**: Tightened `item_debrid_row.xml` vertical spacing and capped passive hero synopsis height in `view_debrid_hero.xml` so row titles remain visible above focused rows. Continue Watching and Trending Movies headings verified visible on `192.168.0.84:5555`; rail, row ordering, card routes, source picker, PlayerActivity, and focus restoration were not changed.
- **Debrid Floating Rail Gutter Correction**: Reduced Debrid main viewport start margin from `168dp` to `144dp` so the icon-only rail reads as a floating overlay instead of leaving a cut/wasted left gutter. Rail remains `84dp` wide at `52dp` start margin, vertically centered, with Search and Discover only.
- **Premium Compact Debrid Sidebar Redesign**: Implemented "A+ Premium Compact Rail" sidebar. Removed `SidebarFocusController` and floating focus pill to resolve z-order and empty-box visual bugs. Switched to stable item-local focus backgrounds using selectors (`bg_debrid_sidebar_item.xml`). Removed broken/stubbed Library, Movies, Series, and duplicate Home buttons. Preserved Search and Discover functionality. Sidebar width fixed to 176dp for professional Android TV readability.
- **MediaFusion/AIO Addon API Exception Fix**: Addressed API exceptions/Rate Limited error streams caused by non-Stremio User-Agent headers. Added OkHttp interceptor and updated request builders to use `User-Agent: Stremio/1.0` and `Accept: application/json` with increased timeout (20s).
- **Debrid Continue Watching Movie Titles Cleaned**: Added a regex cleaner (`cleanDebridTitle` in `DebridViewModel.kt`) to parse raw filenames for movies in the Continue Watching row, hiding long tags like 1080p, WEBRip, brackets, and years. Series titles remain unaffected. Build/install/device launch confirmed on 192.168.0.21.
- **Debrid Hero Phase 1 integration completed**: Existing `view_debrid_hero.xml` reused and added above rows in `fragment_debrid.xml`. `DebridFragment.kt` updates hero from focused `DebridContentItem`. Hero is passive and does not steal focus. Existing row click/detail/source picker/player routes preserved. Build/install/device launch confirmed on 192.168.0.21. Manual QA passed on 192.168.0.21. 192.168.0.84 not tested.
- Debrid focus restoration fix completed. `DebridFragment.kt` updated to persist focus state (`onSaveInstanceState`), resolving conflicts between `onResume` and `updateUI`. Sidebar focus protection preserved. Fallbacks to nearest valid item if last focused card is lost. Manual QA passed on 192.168.0.21. No PlayerActivity, resolver/playback, Continue Watching data logic, Live, VOD, Series, or Episode Browser changes were made.
- Added workaround/fallback for old Debrid Continue Watching item resume fails. Old items with valid hash/magnet but expired proxy URL now skip the direct addon passthrough to force a fresh resolve through Real-Debrid on retry.
- Added API exception text filter and proxy URL validation check for AIOStreams addon sources to prevent playing 5-second API exception error videos.
- Debrid Continue Watching resume now blocks stale/null-expiry saved direct URLs from being treated as safe playback and refreshes from durable hash/magnet or source-profile metadata instead.
- Resolved Debrid playback "Real Debrid config missing" regression caused by Player/Episode Browser changes.
- Companion security hardening for LAN access, PIN validation, and safer URL handling.
- Debrid source profile metadata is preserved into Player intents and Continue Watching where supported.
- Direct Debrid playback identity guard is documented in app patterns.

## Failed Approaches / Avoid
- Do not infer cache readiness from labels such as `RD+`, `cached`, or provider names.
- Do not retry/auto-next on rate limits, auth failures, network failures, or unknown failures.
- Do not put Stremio `manifest.json` URLs into legacy scraper registry storage.
- Do not send direct addon HTTP URLs through app-side Real-Debrid auth.
- Do not trust expired direct addon URLs for resume.

## QA Checklist
- `:app:compileDebugKotlin`
- `:app:assembleDebug`
- Manual QA: Debrid Hero Phase 1 integration completed and passed on 192.168.0.21.

## Last Verified State
- 2026-05-31: Debug-only Debrid playback diagnostics recorder implemented in the mapped source/player handoff path. Build passed (`:app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.21:5555`, and marker-file smoke confirmed local JSONL export under `playback-diagnostics`. Real Debrid movie source picker QA on `.21` captured source list, selected provider/source identity, resolver-backed playback launch, player prepare, track discovery, first frame, and READY without raw URL/magnet/hash/token/header leakage. Direct addon readiness/dead-link event coverage still needs a manual failing direct sample before using the recorder as closure evidence for those cases.
- 2026-05-31: Debrid series/AIO episode source reliability fix verified on `192.168.0.21:5555`. `UnifiedSourceProvider.kt` now keeps distinct same-hash variants when language/provider/source/file-index metadata differs, and `SeriesDetailActivity.kt` now requires the selected episode's real season/episode metadata instead of silently querying episode 1. Build validation passed (`:app:compileDebugKotlin`, `:app:assembleDebug`), the APK was installed on the target device, and manual QA on `Spider-Noir` confirmed episode 1 vs episode 3 resolve to different episode-specific source lists with `S01E01` vs `S01E03` labels and unchanged `DIRECT` badges. No cache-label semantics were changed.
- 2026-05-30: Debrid Continue Watching key alignment completed and verified. `PlayerHistoryManager.kt` now resolves Debrid CW IDs through the same canonical identity path used by watched-state storage, preserving stable TMDB/IMDb keys, removing raw info-hash fallback leakage, and using hardened title/year plus collision-resistant episode fallback keys when metadata is incomplete. `:app:compileDebugKotlin` and `:app:assembleDebug` passed, and the debug APK was installed on `192.168.0.21:5555` and `192.168.0.84:5555`. No source picker, resolver readiness, direct playback, Live TV, or UI layout behavior was changed.
- 2026-05-30: Debrid watched identity hardening completed and verified. `WatchedIdentityBuilder.kt` now provides standalone secondary validation for watched identity parts, rejects URLs/magnets/token or credential-like values, and filters raw torrent info-hashes using 40-char hex and 32-char base32 regex guards. `PlayerHistoryManager.kt` now routes stable watched identity inputs through that sanitizer, preserves existing valid Debrid movie/series key prefixes, includes release-year discrimination for title fallback keys, and adds collision-resistant fallback discriminators when season/episode metadata is missing. `:app:compileDebugKotlin` passed after stopping a stuck Gradle daemon and rerunning, `:app:assembleDebug` passed, APK installed on `192.168.0.84:5555`, and app launch confirmed with a running process. No source picker, resolver, playback readiness, Live TV, or UI behavior was changed.
- 2026-05-29: Source picker Hindi/Multi language ranking fix completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`) after stopping a stale Gradle daemon and rerunning, APK installed on `192.168.0.84:5555`, and device QA covered Debrid series detail -> episode source picker with 398 sources -> Language dropdown -> Hindi selection. Hindi was available, selecting Hindi returned two Hindi-English direct rows, focus moved to the first matching row, direct source playback launched `PlayerActivity`, Back returned to Debrid, and app-process log scan stayed clean: `RawUrl=0`, `Magnet=0`, `TokenParam=0`, `CredentialWord=0`, `LongHash=0`. RD Cached semantics, direct/proxy readiness, source click route, and Debrid UI were not changed.
- 2026-05-29: Debrid source logging hygiene cleanup completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555`, and device QA covered Debrid movie source picker, direct source launch to `PlayerActivity`, Back return to Debrid, and app-process log scan. Final scan counts: `RawUrl=0`, `Magnet=0`, `TokenParam=0`, `CredentialWord=0`, `LongHash=0`. Source fetching/filtering/playback behavior was not changed.
- 2026-05-29: Source picker dynamic RD Cached Type option fix completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555` and `192.168.0.21:5555`. Device QA on `.84` confirmed a direct-only 15-source picker did not offer RD Cached, Type offered `All`, `Direct`, and `Torrent/Add-on`, direct rows stayed visible under `All`/`Direct`, selecting English kept focus stable and still omitted RD Cached, direct playback launched `PlayerActivity`, and Back returned to Debrid. Mixed `VERIFIED_CACHED` positive-option QA remains pending. Redacted recent-log scan still found URL/token/hash-like patterns, so source log hygiene remains an open gap outside this UI filter change.
- 2026-05-29: Source picker direct false-positive filter fix completed. Root cause was movie/series click-time direct readiness marking direct HTTP sources as `VERIFIED_CACHED`; direct readiness is not durable RD cache proof. `SourceFilterUtils.isPlaybackReady` remains strict to `VERIFIED_CACHED`, direct HTTP readiness now stores `DIRECT_STREAM`, and the cached chip only appears when a verified cached source exists. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`) and APK installed on `192.168.0.84:5555`; `192.168.0.21:5555` was unavailable. Device QA confirmed direct-only pickers hide the RD Cached chip, direct links remain visible with the filter off, an AIOStreams not-ready direct source did not launch `PlayerActivity`, repeated language-chip taps did not crash or lose the picker, and Back returned from detail to Debrid/MainActivity. Mixed verified-cached source QA remains pending. Full source-fetch log review still found pre-existing raw provider/server URL logging outside this fix scope.
- 2026-05-29: Debrid direct proxy readiness targeted provider QA passed on `192.168.0.84:5555` after a reproduced movie MediaFusion-labelled bounce-back failure. Fix stayed in `DebridPlaybackRepository.kt`, `MovieDetailActivity.kt`, and `SeriesDetailActivity.kt`: shared readiness classification now includes existing URL/provider/source metadata and playback headers. `clean :app:compileDebugKotlin` and `:app:assembleDebug` passed, APK installed on `.84`. Movie AIOStreams ready source launched `PlayerActivity` and stayed active past 12s; series episode AIOStreams not-ready source did not launch `PlayerActivity` and stayed/recovered in `SeriesDetailActivity` source picker; non-AIO control source launched `PlayerActivity`; Back returned from Player/detail to Debrid; app-process log scan found zero raw URL, magnet, token-param, or long-hash patterns.
- 2026-05-29: Debrid card animation safety QA passed on `192.168.0.84:5555`. Existing debug APK installed successfully; no runtime code was changed. Rapid D-pad stress across Continue Watching and Trending Movies, repeated row switching, Search grid stress, and Discover rail/top-content behavior showed no focus loss, no stuck glow, no wrong recycled glow/title/rating, no visible jank/lag from the pulse animation, no card clipping from scale, readable rating badges and title/year overlays, visible Continue Watching progress, stable rail left/right navigation, and passive hero. Continue Watching OK opened `PlayerActivity` and Back restored Debrid focus; Trending OK opened detail/source picker and Back restored Debrid focus.
- 2026-05-29: Debrid compact hero header polish completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555`, and device QA confirmed hero title is compact and non-focusable, metadata remains readable, missing synopsis leaves no large blank gap, available synopsis stays capped, Continue Watching and Trending Movies headings are visible, floating rail remains unchanged, D-pad left/right rail navigation is stable, Continue Watching OK opens Player and Back restores Debrid focus, Trending OK opens detail/source picker and Back restores Debrid focus, and the 1080p layout has no clipping.
- 2026-05-28: Verification-only Debrid card polish QA on `192.168.0.84:5555` found one route failure while the card visuals themselves held up under stress. Rapid Trending card left/right scrolling for roughly 30 seconds did not show obvious stuck glow, recycling artifacts, or clipping in the sampled screenshots, and the compact rail still behaved visually. However, a Continue Watching series item (`Inspector Rishi`) opened `SeriesDetailActivity` on OK, and Back returned to Home/MainActivity instead of restoring Debrid focus. Build status remained green from the previously passed debug build, and no code was changed in this verification pass. Next action: separate route review for Continue Watching series items before accepting the animation polish as complete.
- 2026-05-28: Debrid hero metadata/long-title polish completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555`, and device QA confirmed title bounds stable for sampled titles up to `Star Wars The Mandalorian And Grogu`, metadata badges visible for available rating/year/type and absent values collapsed, missing synopsis hidden cleanly, hero non-focusable, Continue Watching and Trending Movies headings still visible, left from first content card enters rail and right returns to content, Continue Watching OK opened Player and Back restored Debrid, Trending OK opened detail/source picker and Back restored Debrid focus.
- 2026-05-28: Debrid row heading/spacing polish completed. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555`, and device QA confirmed Continue Watching heading `[304,360][793,425]`, Trending Movies heading `[304,873][745,938]` at top state and `[304,440][745,504]` when focused, hero non-focusable, rail unchanged, left/right rail focus stable, Continue Watching OK opened Player, Trending OK opened detail, and Back restored Debrid focus.
- 2026-05-28: Debrid icon-only floating rail gutter corrected in `fragment_debrid.xml`. Build passed (`clean :app:compileDebugKotlin`, `:app:assembleDebug`), APK installed on `192.168.0.84:5555`, and device QA confirmed rail `[104,388][272,692]`, main viewport `[288,0][1920,1080]`, backdrop `[0,0][1920,1080]`, Search opens, Discover returns top Debrid content focus, D-pad left/right/up/down stable, hero non-focusable, and Continue Watching present.
- 2026-05-28: Premium Compact Debrid Sidebar Redesign implemented. Removed floating pill and broken buttons, switched to stable item-local focus backgrounds (`bg_debrid_sidebar_item.xml`), and preserved Search and Discover actions. Build/install confirmed on 192.168.0.21 and 192.168.0.84.
- 2026-05-28: MediaFusion/AIO addon sources API exception bug diagnosed and fixed by injecting Stremio-compatible User-Agent and Accept headers. Build/install confirmed on 192.168.0.21 and 192.168.0.84. Manual QA confirmed: addon links working and API error no longer appears.
- 2026-05-27: Debrid Continue Watching movie titles are now stripped of long raw filenames (e.g. 1080p, WEBRip, brackets) for a cleaner UI.
- 2026-05-27: Debrid Hero Phase 1 integration completed. Existing `view_debrid_hero.xml` reused and added above rows in `fragment_debrid.xml`. `DebridFragment.kt` updates hero from focused `DebridContentItem`. Hero is passive and does not steal focus. Existing row click/detail/source picker/player routes preserved. Build/install/device launch confirmed on 192.168.0.21. Manual QA passed on 192.168.0.21. 192.168.0.84 not tested.
- 2026-05-27: Debrid focus restoration fix completed and verified. Manual QA passed on 192.168.0.21. Focus restores safely after returning from detail/player and after Content refresh without stealing focus from sidebar.
- 2026-05-27: Old Debrid Continue Watching proxy/direct URL retry fix has been applied. Build and install completed. Fix is PENDING next-day QA to confirm no stuck loading and proper fresh resolving.
- 2026-05-25: Post-refactor full app smoke test on 192.168.0.84. Debrid source picker PARTIAL (MediaFusion/AIO intermittent API exceptions, StremThru PASS). Debrid CW resume INTERMITTENT. Debrid CW expired resume FAIL (starts then stalls on reloading). These are pre-existing issues, not caused by PlayerActivity refactoring. Open Issues A/B/C recorded above. No fixes applied.
- 2026-05-24: `:app:compileDebugKotlin` and `:app:assembleDebug` passed after the Continue Watching resume branch fix. Debug APK installed and `MainActivity` launched on `192.168.0.21:5555` and `192.168.0.84:5555`; app PIDs confirmed.
- 2026-05-24 follow-up: expired/null `expiresAt` Debrid movie Continue Watching resume passed on `192.168.0.21:5555`. Existing null-expiry Debrid history item fresh-resolved through `PlaybackResolver` and started playback without stale URL replay or false Real-Debrid config missing.
- Module remains partial because full Stremio/provider parity and some cache verification need manual QA.
# #   S e s s i o n   2 0 2 6 - 0 5 - 2 8 :   D e b r i d   C o n t i n u e   W a t c h i n g   S e r i e s   B a c k   R e s t o r e   F i x 
 -   F i x e d   a   n a v i g a t i o n   b u g   w h e r e   p r e s s i n g   B a c k   f r o m   S e r i e s D e t a i l A c t i v i t y   a f t e r   o p e n i n g   a   D e b r i d   C o n t i n u e   W a t c h i n g   s e r i e s   i t e m   r e t u r n e d   t h e   u s e r   t o   t h e   M a i n A c t i v i t y   H o m e   t a b   i n s t e a d   o f   r e s t o r i n g   D e b r i d   f r a g m e n t   f o c u s . 
 -   S o l u t i o n :   I m p l e m e n t e d   e x p l i c i t   o n B a c k P r e s s e d ( )   h a n d l i n g   i n   S e r i e s D e t a i l A c t i v i t y   t o   c a l l    i n i s h ( )   s p e c i f i c a l l y   w h e n   i s D e b r i d C o n t e n t   i s   t r u e .   T h i s   b y p a s s e s   t h e   d e f a u l t   U p - n a v i g a t i o n   r e c r e a t i o n   o f   M a i n A c t i v i t y   c a u s e d   b y    n d r o i d : p a r e n t A c t i v i t y N a m e .  
 
- 2026-05-29: Source picker Language dropdown now dynamically populated from active sources only. Hardcoded mock list removed. Unknown/Multi handled dynamically.
- 2026-05-28: MediaFusion/AIO addon sources API exception bug diagnosed and fixed by injecting Stremio-compatible User-Agent and Accept headers. Build/install confirmed on 192.168.0.21 and 192.168.0.84. Manual QA confirmed: addon links working and API error no longer appears.
- 2026-05-27: Debrid Continue Watching movie titles are now stripped of long raw filenames (e.g. 1080p, WEBRip, brackets) for a cleaner UI.
- 2026-05-27: Debrid Hero Phase 1 integration completed. Existing `view_debrid_hero.xml` reused and added above rows in `fragment_debrid.xml`. `DebridFragment.kt` updates hero from focused `DebridContentItem`. Hero is passive and does not steal focus. Existing row click/detail/source picker/player routes preserved. Build/install/device launch confirmed on 192.168.0.21. Manual QA passed on 192.168.0.21. 192.168.0.84 not tested.
- 2026-05-27: Debrid focus restoration fix completed and verified. Manual QA passed on 192.168.0.21. Focus restores safely after returning from detail/player and after Content refresh without stealing focus from sidebar.
- 2026-05-27: Old Debrid Continue Watching proxy/direct URL retry fix has been applied. Build and install completed. Fix is PENDING next-day QA to confirm no stuck loading and proper fresh resolving.
- 2026-05-25: Post-refactor full app smoke test on 192.168.0.84. Debrid source picker PARTIAL (MediaFusion/AIO intermittent API exceptions, StremThru PASS). Debrid CW resume INTERMITTENT. Debrid CW expired resume FAIL (starts then stalls on reloading). These are pre-existing issues, not caused by PlayerActivity refactoring. Open Issues A/B/C recorded above. No fixes applied.
- 2026-05-24: `:app:compileDebugKotlin` and `:app:assembleDebug` passed after the Continue Watching resume branch fix. Debug APK installed and `MainActivity` launched on `192.168.0.21:5555` and `192.168.0.84:5555`; app PIDs confirmed.
- 2026-05-24 follow-up: expired/null `expiresAt` Debrid movie Continue Watching resume passed on `192.168.0.21:5555`. Existing null-expiry Debrid history item fresh-resolved through `PlaybackResolver` and started playback without stale URL replay or false Real-Debrid config missing.
- Module remains partial because full Stremio/provider parity and some cache verification need manual QA.
# #   S e s s i o n   2 0 2 6 - 0 5 - 2 8 :   D e b r i d   C o n t i n u e   W a t c h i n g   S e r i e s   B a c k   R e s t o r e   F i x 
 -   F i x e d   a   n a v i g a t i o n   b u g   w h e r e   p r e s s i n g   B a c k   f r o m   S e r i e s D e t a i l A c t i v i t y   a f t e r   o p e n i n g   a   D e b r i d   C o n t i n u e   W a t c h i n g   s e r i e s   i t e m   r e t u r n e d   t h e   u s e r   t o   t h e   M a i n A c t i v i t y   H o m e   t a b   i n s t e a d   o f   r e s t o r i n g   D e b r i d   f r a g m e n t   f o c u s . 
 -   S o l u t i o n :   I m p l e m e n t e d   e x p l i c i t   o n B a c k P r e s s e d ( )   h a n d l i n g   i n   S e r i e s D e t a i l A c t i v i t y   t o   c a l l    i n i s h ( )   s p e c i f i c a l l y   w h e n   i s D e b r i d C o n t e n t   i s   t r u e .   T h i s   b y p a s s e s   t h e   d e f a u l t   U p - n a v i g a t i o n   r e c r e a t i o n   o f   M a i n A c t i v i t y   c a u s e d   b y     n d r o i d : p a r e n t A c t i v i t y N a m e .  
 
- 2026-05-29: Source picker Language dropdown now dynamically populated from active sources only. Hardcoded mock list removed. Unknown/Multi handled dynamically.
- 2026-05-29: Source picker UI polished. Panel background made 98% solid dark to prevent detail page bleed. Focused row styling switched from full blue fill to cyan 2dp border with dark bg. Footer hints overlap fixed by removing clipToPadding. Header aligned cleanly.
- 2026-05-29: Source picker final UI polish completed. Reduced focused row glow intensity, removed layout scaling jump, kept normal rows readable (alpha 0.85), updated filter chips to pill shapes, and increased vertical space to fit 4 source rows on 1080p. Tested and passed on .21 and .84 devices.
- 2026-05-29: Source picker final UI tuning completed. Made panel 100% solid dark, removed cyan glow and reduced translationZ to 4f, kept normal rows bright and separated footer with 1dp border. Tested and passed on .21 and .84 devices.
- 2026-05-29: Fixed language filter bug in source picker. Filter now correctly translates selected dropdown label to matching language codes from source metadata using SourceFilterUtils.mapLanguageCodeToName, preventing generic MULTI sources from showing up under specific language searches.
- 2026-05-29: Fixed 'No additional sources available' regression in source picker. Separated UI 'preferredLanguage' filtering from global 'sortLanguage' preferences in SourceFilterState so that global audio preferences do not aggressively filter out mismatched sources before the user opens the picker.
- 2026-05-29: Fixed source picker scrolling bug where the list would jump back to the top when navigating past the first few visible rows. The bug was caused by duplicate streamIds breaking RecyclerView focus with hasStableIds(true). Appended index to streamIds in UnifiedSourceProvider.kt to ensure uniqueness.
- 2026-05-29: Fixed PlayerActivity BACK button crash that caused navigation to return directly to Home. The crash was an `UninitializedPropertyAccessException` in `SourceSelectionBottomSheet` because `showSources` was called via `ActivityResultCallback` before `onViewCreated` finished view inflation. Fix safely defers source updating to `onViewCreated` via `pendingSources`. Manual QA confirmed BACK navigation now correctly resumes the source picker/detail flow.
- 2026-05-31: Phase 1 debrid-agnostic playback amplification fix verified and closed. Direct addon/proxy HTTP `500-599` now retries once at most for the same URL fingerprint plus effective headers, then returns picker-launched playback to the source list. Device diagnostics on `192.168.0.21:5555` recorded `500 -> retry 1 -> 500 -> terminal_failure -> return_to_sources(autoNext=false)` with unchanged fingerprint and `stremio` header profile. HTTP `404` still fast-fails with zero retries. Regression QA on `192.168.0.84:5555` recorded AIO series direct READY, a second post-READY AIO launch READY with zero retries, StreamThru series direct READY, and StreamThru movie direct READY.
- 2026-05-31: Session-only direct addon/proxy ranking now records positive preference only after first frame plus credible duration (`>=30m` movie, `>=5m` episode/series). This prevents short error payloads from being treated as known-good rows while preserving visibility and playback routing. Build passed and refreshed APK installed on `192.168.0.84:5555`. Device QA on `From` S4E1 recorded valid AIO READY at `3239952ms`, rejected a StreamThru short payload at `30000ms`, and confirmed unchanged 25-row picker visibility. Movie direct QA recorded READY at `6329376ms`.
- 2026-06-11: Optimized and re-styled `DropdownAdapter` in `DebridDiscoverActivity` for Android TV. Integrated high-contrast text state list (Black on focused, White on unfocused) and dynamic background state list (Cyan focused highlight, dark surface normal state), adjusted padding to 28x20dp and font size to 16f, and preserved safe Jakarta typeface. Build compilation successfully validated via `:app:compileDebugKotlin`.
- 2026-06-11: Repaired the item click handler in `showDropdown()` of `DebridDiscoverActivity.kt` to immediately dismiss the popup layer, request focus back on the anchor button (critical for D-pad stability), and sanitize the selected label by replacing the `\u2713 ` checkmark character before invoking `onSelected`. Verified build compilation and installed the APK to `192.168.178.35:5555` successfully.


