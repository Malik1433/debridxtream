<?xml version="1.0" encoding="utf-8"?>
<!--
    Destructive action. Red is present at rest (tinted fill + red edge) so the danger reads before
    focus lands, and the focused state fills solid red with a pale ring — a different ring colour
    from Cancel, so the two never look alike at a glance.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFF3366" />
            <stroke android:width="2dp" android:color="#FFFFD3DC" />
            <corners android:radius="10dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFE02E5A" />
            <stroke android:width="1dp" android:color="#FFFF3366" />
            <corners android:radius="10dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#33FF3366" />
            <stroke android:width="1dp" android:color="#99FF3366" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</selector>
