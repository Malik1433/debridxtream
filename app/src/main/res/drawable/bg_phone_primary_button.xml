<?xml version="1.0" encoding="utf-8"?>
<!-- The only filled element on the screen, so Play is never in doubt. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="10dp" />
    <gradient
        android:angle="0"
        android:startColor="@color/phone_blue"
        android:endColor="@color/phone_cyan"
        android:type="linear" />
</shape>
