<?xml version="1.0" encoding="utf-8"?>
<!-- FIX 3: neutral rounded tile behind channel logos — replaces the old
     per-channel hashed-color gradient (which read as a "colored border" around
     the real logo image). Same fill/border for every row, no per-channel color. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#1F2937" />
    <stroke android:width="1dp" android:color="#0DFFFFFF" />
    <corners android:radius="8dp" />
</shape>
