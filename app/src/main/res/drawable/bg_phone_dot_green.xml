<?xml version="1.0" encoding="utf-8"?>
<!-- The CACHED dot. Green means "this will play now", which is a different claim from the cyan
     accent, so it gets its own colour rather than borrowing the brand one. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="@color/phone_green" />
</shape>
