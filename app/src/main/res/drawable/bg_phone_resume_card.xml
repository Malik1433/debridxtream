<?xml version="1.0" encoding="utf-8"?>
<!-- Resume-last-channel card: a panel with a cyan hairline, so it reads as an offer rather
     than as the first row of the list. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/phone_panel" />
    <stroke android:width="1dp" android:color="#2E00F0FF" />
</shape>
