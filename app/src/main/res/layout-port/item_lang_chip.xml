<?xml version="1.0" encoding="utf-8"?>
<!--
  M5: the language chip on a PHONE. Same two ids, same kinds — only the type scale
  changes. The landscape chip is 9sp/7sp, which is the px÷2 trick for a 10-foot screen
  read from across a room; held at arm's length, 7sp is a smudge. The "HI ✓" mark that
  H4 earned has to be legible or it may as well not be there.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="6dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_flag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/tv_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="3dp"
        android:textColor="#94A3B8"
        android:textSize="11sp"
        android:textStyle="bold" />
</LinearLayout>
