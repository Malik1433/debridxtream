<?xml version="1.0" encoding="utf-8"?>
<!--
  One option inside a settings picker sheet (G3).

  52dp so the whole row is the target — a tick you have to hit is a 20dp target pretending to be a
  control. The tick is on the trailing edge because the label is what the eye scans.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp">

    <TextView
        android:id="@+id/settings_pick_row_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="@font/inter_regular"
        android:maxLines="1"
        android:textColor="@color/phone_text_primary"
        android:textSize="15sp"
        tools:text="English (EN)" />

    <ImageView
        android:id="@+id/settings_pick_row_tick"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="12dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_stremio_check"
        android:visibility="gone"
        app:tint="@color/phone_cyan"
        tools:visibility="visible" />
</LinearLayout>
