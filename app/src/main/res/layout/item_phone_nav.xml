<?xml version="1.0" encoding="utf-8"?>
<!-- One bottom-navigation destination. 64dp tall, so the whole cell is the target, not the icon. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:minWidth="48dp">

    <View
        android:id="@+id/phone_nav_indicator"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_gravity="top"
        android:background="@color/phone_cyan"
        android:visibility="gone"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/phone_nav_icon"
            android:layout_width="21dp"
            android:layout_height="21dp"
            android:contentDescription="@null"
            tools:src="@drawable/ic_stremio_home" />

        <TextView
            android:id="@+id/phone_nav_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:fontFamily="@font/outfit_semibold"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textSize="12sp"
            tools:text="Home" />
    </LinearLayout>
</FrameLayout>
