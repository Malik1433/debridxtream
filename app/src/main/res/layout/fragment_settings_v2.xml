<?xml version="1.0" encoding="utf-8"?>
<!--
  PHONE Settings — portrait.

  The television version is untouched in layout-television/: a 330dp category rail beside a detail
  panel is right at three metres. On a 411dp phone those two columns cost 144dp of width before a
  single setting is visible, which is how this screen became unusable in the hand.

  Same ids, same kinds, one column: a 56dp app bar, the categories as a chip rail across the top
  (the code already switches the rail's orientation on `settings_categories_are_horizontal`), the
  panel's own heading, and the settings themselves filling everything below.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/phone_home_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- ══ APP BAR ══ the same 56dp bar every other phone screen wears. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_settings"
                app:tint="@color/phone_cyan" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:fontFamily="@font/outfit_extrabold"
                android:letterSpacing="-0.02"
                android:text="@string/settings"
                android:textColor="@color/phone_text_primary"
                android:textSize="19sp" />

            <TextView
                android:id="@+id/tv_settings_version"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/share_tech_mono"
                android:letterSpacing="0.1"
                android:maxLines="1"
                android:textColor="@color/phone_text_muted"
                android:textSize="11sp"
                tools:text="DX PLAY · v2.6.6" />
        </LinearLayout>

        <!-- ══ CATEGORIES ══ a chip rail, because a column of them would take the screen. -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_settings_categories"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:paddingHorizontal="16dp"
            android:paddingBottom="8dp"
            android:scrollbars="none" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/phone_panel" />

        <!-- ══ WHAT THIS SECTION IS ══ -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingTop="16dp">

            <View
                android:id="@+id/v_panel_dot"
                android:layout_width="3dp"
                android:layout_height="16dp"
                android:background="@color/phone_cyan" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="9dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_panel_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:fontFamily="@font/outfit_bold"
                    android:maxLines="1"
                    android:textColor="@color/phone_text_primary"
                    android:textSize="16sp"
                    tools:text="Playback" />

                <TextView
                    android:id="@+id/tv_panel_desc"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:ellipsize="end"
                    android:fontFamily="@font/share_tech_mono"
                    android:letterSpacing="0.08"
                    android:maxLines="1"
                    android:textColor="@color/phone_text_muted"
                    android:textSize="11.5sp"
                    tools:text="AUDIO · LANGUAGE" />
            </LinearLayout>
        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_settings_details"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:paddingHorizontal="16dp"
            android:paddingBottom="24dp"
            android:scrollbars="none" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
