<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="@bool/rows_focusable_in_touch_mode"
    android:padding="4dp"
    android:clipToPadding="false"
    android:clipChildren="false">

    <!-- Focus glow: app-default cyan bloom. MUST extend 12dp beyond the poster so the
         inset blur spills outward (matches home cards). Drawable set in code. -->
    <View
        android:id="@+id/glow_focus"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="-12dp"
        android:duplicateParentState="true"
        app:layout_constraintTop_toTopOf="@id/card_container"
        app:layout_constraintBottom_toBottomOf="@id/card_container"
        app:layout_constraintStart_toStartOf="@id/card_container"
        app:layout_constraintEnd_toEndOf="@id/card_container" />

    <!-- ─── POSTER ─── -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="2:3"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        android:duplicateParentState="true"
        android:foreground="@drawable/card_focus_cinematic"
        app:cardBackgroundColor="@color/card_background">

        <!-- Poster Image -->
        <ImageView
            android:id="@+id/iv_movie_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Movie Poster"
            android:scaleType="centerCrop" />

        <!-- Quality Badge (top-right) -->
        <LinearLayout
            android:id="@+id/ll_quality_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:background="@drawable/bg_badge_quality"
            android:orientation="horizontal"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:gravity="center">

            <TextView
                android:id="@+id/tv_quality_value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="HD"
                android:textColor="@color/vod_accent_cyan"
                android:textSize="9sp"
                android:textStyle="bold"
                android:fontFamily="monospace"
                android:letterSpacing="0.04" />
        </LinearLayout>

        <!-- Watched badge (top-left) -->
        <FrameLayout
            android:id="@+id/fl_watched_badge"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="top|start"
            android:layout_margin="8dp"
            android:background="@drawable/bg_watched_badge"
            android:visibility="gone">

            <ImageView
                android:id="@+id/iv_watched_check"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_check_circle"
                app:tint="@color/vod_green"
                android:contentDescription="@string/a11y_watched" />
        </FrameLayout>

        <!-- Favorite indicator (hidden by default) -->
        <ImageView
            android:id="@+id/iv_favorite_indicator"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="top|start"
            android:layout_margin="4dp"
            android:padding="5dp"
            android:src="@drawable/ic_favorite"
            app:tint="@color/vod_accent_cyan"
            android:background="@drawable/bg_circle_dark_translucent"
            android:visibility="gone"
            android:contentDescription="@string/a11y_in_favorites" />

        <!-- Progress bar (bottom of poster, for in-progress movies) -->
        <FrameLayout
            android:id="@+id/fl_progress_bar"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_gravity="bottom"
            android:visibility="gone">

            <View
                android:id="@+id/progress_bar_track"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#1AFFFFFF" />

            <View
                android:id="@+id/progress_bar_fill"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:background="@drawable/bg_progress_fill" />
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- ─── META (below poster): title only — a calm grid. Rating/year/genre for the
         focused card show in the section header (updateSectionHeaderForMovie). ─── -->
    <TextView
        android:id="@+id/tv_movie_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="9dp"
        android:maxLines="@integer/vod_card_title_lines"
        android:ellipsize="end"
        android:text="Movie Title"
        android:textColor="#E2E8F0"
        android:textSize="@dimen/vod_card_title_size"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toBottomOf="@id/card_container"
        app:layout_constraintStart_toStartOf="@id/card_container"
        app:layout_constraintEnd_toEndOf="@id/card_container" />

</androidx.constraintlayout.widget.ConstraintLayout>
