<?xml version="1.0" encoding="utf-8"?>
<!--
  VOD Player redesign: BIG episode card for a centered carousel (focused card centered & full,
  neighbours peek half above/below). Preserves ViewHolder ids.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="164dp"
    android:orientation="vertical"
    android:focusable="true"
    android:clickable="true"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:padding="4dp">

    <!-- big thumbnail -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cv_thumb"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#12202A">

        <ImageView
            android:id="@+id/iv_episode_thumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            tools:src="@tools:sample/backgrounds/scenic" />

        <LinearLayout
            android:id="@+id/layout_episode_placeholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_episode_placeholder"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">
            <TextView
                android:id="@+id/tv_placeholder_episode_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:textColor="@color/neon_cyan"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="E01" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/gradient_overlay_bottom" />

        <!-- episode number badge -->
        <TextView
            android:id="@+id/tv_episode_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_margin="8dp"
            android:background="@drawable/bg_episode_badge"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:fontFamily="monospace"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            tools:text="E01" />

        <!-- playing / watched badge -->
        <TextView
            android:id="@+id/tv_playing_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:background="@drawable/bg_vodp_now_badge"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:fontFamily="monospace"
            android:textStyle="bold"
            android:text="▶ NOW"
            android:textColor="@color/vodp_now_badge_text"
            android:textSize="9sp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- duration badge -->
        <TextView
            android:id="@+id/tv_episode_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="8dp"
            android:background="@drawable/bg_continue_type_badge"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:fontFamily="monospace"
            android:textColor="#E7EDF3"
            android:textSize="9sp"
            tools:text="42m" />

    </androidx.cardview.widget.CardView>

    <!-- title + meta -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:textStyle="bold"
        android:textColor="@color/stremio_text_primary"
        android:textSize="13sp"
        tools:text="The Name of the Game" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_episode_meta"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:textColor="@color/stremio_text_muted"
            android:textSize="9sp"
            tools:text="Season 1" />

        <ProgressBar
            android:id="@+id/progress_episode"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="90dp"
            android:layout_height="2dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/bg_vodp_countdown_bar"
            android:visibility="gone"
            tools:visibility="visible"
            tools:progress="60" />

    </LinearLayout>

</LinearLayout>
