<?xml version="1.0" encoding="utf-8"?>
<!-- One channel row of the in-player TV-Guide grid (Live Player v2):
     fixed channel column + a timeline that holds time-positioned show blocks. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:orientation="horizontal">

    <!-- channel column -->
    <LinearLayout
        android:id="@+id/guide_ch_col"
        android:layout_width="140dp"
        android:layout_height="match_parent"
        android:background="@drawable/bg_live_guide_ch_col"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="11dp"
        android:paddingEnd="9dp">

        <FrameLayout
            android:id="@+id/guide_ch_logo"
            android:layout_width="40dp"
            android:layout_height="40dp">

            <TextView
                android:id="@+id/guide_ch_logo_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textColor="@android:color/white"
                android:textSize="9sp"
                android:textStyle="bold"
                tools:text="ARD" />

            <ImageView
                android:id="@+id/guide_ch_logo_img"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="2dp"
                android:scaleType="fitCenter"
                android:visibility="gone"
                android:contentDescription="@null" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/guide_ch_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/plus_jakarta_sans"
                android:maxLines="1"
                android:textColor="@color/live_osd_text_soft"
                android:textSize="9sp"
                android:textStyle="bold"
                tools:text="SKY SPORT 1" />

            <TextView
                android:id="@+id/guide_ch_num"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:fontFamily="monospace"
                android:textColor="@color/live_osd_text_faint"
                android:textSize="6.5sp"
                tools:text="003" />
        </LinearLayout>
    </LinearLayout>

    <!-- timeline (show blocks positioned by air-time in code) -->
    <FrameLayout
        android:id="@+id/guide_row_timeline"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1" />
</LinearLayout>
