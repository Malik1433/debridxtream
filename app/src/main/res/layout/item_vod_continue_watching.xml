<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:visibility="gone">

    <!-- Section title row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="44dp"
        android:paddingEnd="52dp"
        android:paddingTop="20dp"
        android:paddingBottom="13dp">

        <!-- Accent bar -->
        <View
            android:layout_width="3dp"
            android:layout_height="18dp"
            android:background="@color/vod_accent_cyan" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:text="@string/section_continue_watching"
            android:textColor="@color/vod_text_primary"
            android:textSize="17sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-condensed" />

        <TextView
            android:id="@+id/tv_cw_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@string/ui_0_in_progress"
            android:textColor="@color/vod_text_faint"
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:letterSpacing="0.06" />
    </LinearLayout>

    <!-- Horizontal cards -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_continue_watching"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingStart="44dp"
        android:paddingEnd="52dp"
        android:paddingBottom="12dp"
        android:scrollbars="none" />
</LinearLayout>
