/**\n * Channel Card Component\n * Displays individual TV channel with logo, views, quality badges, and interactive elements\n * Optimized for Android TV with large touch targets and focus indicators\n */\n\nimport React from 'react';\nimport { Star } from 'lucide-react';\nimport { Show } from '../data/channels';\n\ninterface ChannelCardProps {\n  show: Show;\n  onSelect?: (show: Show) => void;\n  isFocused?: boolean;\n}\n\nexport function ChannelCard({ show, onSelect, isFocused = false }: ChannelCardProps) {\n  return (\n    <div\n      onClick={() => onSelect?.(show)}\n      className=\"relative group cursor-pointer\"\n    >\n      {/* Card Background - TV Optimized Size */}\n      <div\n        className={`\n          relative rounded-2xl overflow-hidden\n          bg-slate-800/50 backdrop-blur-sm\n          border-2 border-slate-700/60 transition-all duration-200\n          p-6 flex items-center justify-between h-36\n          ${\n            isFocused\n              ? 'ring-4 ring-yellow-400 shadow-2xl shadow-yellow-400/50 bg-slate-800/80 border-yellow-400'\n              : 'hover:bg-slate-800/70 hover:ring-2 hover:ring-blue-400/60'\n          }\n        `}\n      >\n        {/* Left Section - Icon/Logo + Channel Name */}\n        <div className=\"flex-1 flex items-center gap-6 min-w-0\">\n          {/* Icon - Larger for TV */}\n          <div className=\"text-6xl flex-shrink-0\">📺</div>\n\n          {/* Channel Info */}\n          <div className=\"flex-1 min-w-0\">\n            <h3 className=\"text-white font-bold text-2xl truncate\">{show.name}</h3>\n            <p className=\"text-slate-400 text-lg truncate mt-1\">{show.views} Views</p>\n          </div>\n        </div>\n\n        {/* Right Section - Quality Badges + Star */}\n        <div className=\"flex items-center gap-4 flex-shrink-0 ml-4\">\n          {/* Quality Badges */}\n          <div className=\"flex gap-3 items-center\">\n            {show.quality.map((q) => (\n              <span\n                key={q}\n                className={`\n                  px-4 py-2 rounded-lg text-base font-bold whitespace-nowrap\n                  ${\n                    q === '4K'\n                      ? 'bg-red-900/80 text-red-100'\n                      : q === 'HD'\n                        ? 'bg-blue-900/80 text-blue-100'\n                        : q === 'EPG'\n                          ? 'bg-slate-700/80 text-slate-100'\n                          : 'bg-yellow-900/80 text-yellow-100'\n                  }\n                `}\n              >\n                {q}\n              </span>\n            ))}\n          </div>\n\n          {/* Favorite Star */}\n          {show.favorite && (\n            <Star\n              size={28}\n              className=\"fill-yellow-400 text-yellow-400 flex-shrink-0 drop-shadow-lg\"\n              strokeWidth={2}\n            />\n          )}\n        </div>\n      </div>\n    </div>\n  );\n}\n"
}
